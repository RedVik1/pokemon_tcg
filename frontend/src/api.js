// Re-exported from shared/api/axios.js — use shared/ directly in new code
export {
  default,
  login,
  register,
  register as registerUser,
  addCardToVault,
  removeCollectionItem,
} from "./shared/api/axios";
